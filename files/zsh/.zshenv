# Disable the global compinit from Debian's system-wide /etc/zsh/zshrc
# Compinit is already done by Oh-My-Zsh
skip_global_compinit=1

# Move all zsh config files into .config directory
ZDOTDIR="$HOME/.config/zsh"
